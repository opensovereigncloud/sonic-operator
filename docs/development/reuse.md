# REUSE compliance

This project follows the REUSE specification. The configuration is in `REUSE.toml`.

## Checks
- License headers:

```sh
make check-license
```

- REUSE lint (requires the `reuse` CLI):

```sh
reuse lint
```
